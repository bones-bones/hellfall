import { createStyles } from '@workday/canvas-kit-styling';

// Label
export const labelStyles = createStyles({ fontWeight: 'bold' });

// legend
export const legendStyles = createStyles({ fontWeight: 'bold' });

// Box
export const componentHolderStyles = createStyles({
  display: 'flex',
  flexDirection: 'column',
  marginTop: '10px',
});
