#!/usr/bin/env sh

# GitHub Desktop (and other GUI git clients) use a minimal PATH that
# omits Homebrew bins like yarn and node.
export PATH="/usr/local/bin:/opt/homebrew/bin:$PATH"

yarn pretty
yarn lint
git add -u
yarn typecheck
